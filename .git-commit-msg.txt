polish + hardening: bloom, per-map exposure, pause, Codex review fixes

Visual: UnrealBloom (solo), per-map tone exposure (golden dunes, lava
glow, neon grid pop), richer palettes, stronger wireframe, lava raised
into its channels, sprite pool 140->300 (nuke headroom). All five maps
+ action frames screenshot-verified.

Game shell: Esc pause overlay (resume / abandon), continuous win-
condition checking, README with controls/tanks/maps/rounds + hero
shots in docs/.

Codex CLI adversarial review — all 9 findings fixed:
- CRITICAL stale finish timer could dispose a NEWER match after
  re-deploy (timer cleared + disposed guard)
- GPU disposal ordering leaked shell/crate geometry every match;
  weapons.dispose() added, textures disposed
- Input listeners leaked per match (Input.dispose())
- AI targeting now honest free-for-all (team colors cosmetic)
- self-nuke deals damage but credits no kill; killfeed shows OWN GOAL
- respawns reject cliffs, water/lava, props, burning ground
- hold-to-fire documented as intentional artillery cadence
- effects sprite budget covers chained nukes

Testing: playtest now steps the simulation deterministically
(window.__TEST_MANUAL decouples game.update from SwiftShader frame
rate) — 26 checks across 5 maps + a 4-minute simulated 8-tank war
through to the victory screen. Full double verification (screenshots
+ playtest, twice): ALL GREEN both passes, zero console errors.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
