menus with live previews, full gamepad support + remapping, graphics tier 2

Menus: every selection now shows a picture — tank select renders real
3/4 hero shots of each chassis on a plinth, map select renders aerial
vistas of each world (src/thumbs.js, offscreen renderer, cached data
URLs — previews can never drift from the actual game).

Gamepad (Codex CLI module + integration): plug-and-play on both
players (pad 0 = P1, pad 1 = P2). Left stick drives, right stick aims
turret + elevation, RT fires, LT machine-guns, Start pauses. Pads
also navigate every menu (D-pad/stick flicks + A/B). OPTIONS screen:
rebind fire/MG/pause (press-to-capture), invert aim Y, reset layout —
persisted in localStorage. Master + music volume sliders, persisted.

Graphics tier 2: image-based environment lighting (RoomEnvironment
PMREM — armor finally reads as metal), billboard cloud layers on
daylight maps, animated water swell + lava/energy heat-pulse,
2,600 instanced grass tufts on Verdant Vale, procedural micro-noise
detail texture on all terrain, wheels that actually spin.

Gameplay: bots now hold area specials (nuke/inferno/scatter) until
outside their own blast radius — no more self-deletion; AP splash up;
CDN import map (jsdelivr) so the game runs anywhere, GitHub Pages
included.

Verified: screenshot suite (13 shots incl. tank/map/options menus,
zero console errors) + full playtest ALL GREEN.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
